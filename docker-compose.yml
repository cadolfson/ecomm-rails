version: '3'

services:
   db:
     image: postgres
     volumes:
       - db:/var/lib/postgresql/data
     restart: always

   rails:
     depends_on:
       - db
     image: cadolfson/ecomm-rails
     ports:
       - "3000:3000"
     
volumes:
    db: