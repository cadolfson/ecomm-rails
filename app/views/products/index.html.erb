<h1>All Products</h1>
<p>Here is what we have to offer.</p>

<ul>
    <% @products.each do | product |%>
        <li>
           <%= image_tag product.image.url, size: "100" %>
           <h2><%= product.name %><h2>
        </li>
    <% end %>